# Crestline Pay

A polished, multi-page banking website for Crestline Pay with a premium fintech look.

## Preview locally

Open the project in a browser or run:

```bash
python -m http.server 8000
```

Then visit http://localhost:8000
